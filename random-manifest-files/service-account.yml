apiVersion: v1
kind: ServiceAccount
metadata: 
  name: my-service-account
  namespace: default

------ 

# use that service account in a pod

apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app.kubernetes.io/name: myapp
spec:
  serviceAccountName: my-service-account
  containers:
  - name: myapp
    image: <Image>